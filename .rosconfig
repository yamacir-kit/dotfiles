#!/bin/bash

ros_version="indigo"

# catkin_ws="$HOME/works/kv2_colored_shape_tracker"
# catkin_ws="$HOME/works/pubsub"
catkin_ws="$HOME/works/arcsys2"
# catkin_ws="$HOME/works/tutorials/rosik"

source /opt/ros/$ros_version/setup.bash
source $catkin_ws/devel/setup.bash

alias cdc='cd $catkin_ws'

function catkin_make() {
  (cd $catkin_ws && command catkin_make $@) && source $catkin_ws/devel/setup.bash && rospack profile
}
