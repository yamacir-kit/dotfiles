#!/bin/bash

export ROS_VERSION="indigo"
source /opt/ros/${ROS_VERSION}/setup.bash

export CATKIN_WORKSPACE="$HOME/works/pubsub"
alias cdc='cd ${CATKIN_WORKSPACE}'

#alias catkin_make='cd ${CATKIN_WORKSPACE} && source devel/setup.bash && catkin_make -DCMAKE_BUILD_TYPE=RelWithDebInfo; cd -'

function catkin_make() {
  (cd ${CATKIN_WORKSPACE} && command catkin_make $@) && source devel/setup.bash && cd -;
}
