on:
  push:
  schedule:
    - cron: 0 0 * * *
jobs:
  Setup:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-24.04, macos-14, macos-15, macos-26 ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - run: ./install.sh
      - run: ./scripts/setup-${{ matrix.os }}.sh
      - run: ./uninstall.sh
